import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amalprasad.app',
  appName: 'Dev-Amal',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
